<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <label>Nome</label>
    <%= form.text_field :name, id: :restaurant_name, :class => "form-control" %>
  </div>

  <div class="field form-group">
    <label>Endereço</label>
    <%= form.text_field :address, id: :restaurant_address, :class => "form-control" %>
  </div>

  <div class="field form-group">
    <label>Número de Telefone</label>
    <%= form.text_field :phonenumber, id: :restaurant_phonenumber, :class => "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit submit_name, :class => "btn btn-default" %>
  </div>
<% end %>
